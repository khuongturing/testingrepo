import {
  address_1,
  address_2,
  city,
  region,
  postal_code,
  country,
  shipping_region_id,
} from './validationFields/customer';

const schema = {
  address_1,
  address_2,
  city,
  region,
  postal_code,
  country,
  shipping_region_id,
};

export default schema;
